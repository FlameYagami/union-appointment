# 开发环境配置
server:
  # 服务器的HTTP端口
  port: 8100

# Swagger配置
swagger:
  show: false
  basePackage: com.gk

# 日志配置
logging:
  #引入
  config: classpath:config/log4j2-prod.yml

# 基础URL
baseUrl:
  host: http://domain.com/

# 基础文件路径
basePath:
  file: D:/union-appointment/files/
